# ── DeepWebHarvester — environment variable template ─────────────────────────
#
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# NEVER commit your .env file to version control.

# Tor control port password (plain text — matches what you hashed with tor --hash-password)
TOR_CONTROL_PASSWORD=your_strong_password_here

# Tor SOCKS5 proxy port (default: 9050)
# TOR_SOCKS_PORT=9050

# Tor control port (default: 9051)
# TOR_CONTROL_PORT=9051

# Logging verbosity: DEBUG | INFO | WARNING | ERROR (default: INFO)
# LOG_LEVEL=INFO

# Override output directory (default: results/)
# OUTPUT_DIR=results
